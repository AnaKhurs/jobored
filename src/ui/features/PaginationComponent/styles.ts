import {createStyles, rem} from "@mantine/core";

export const useStyles = createStyles((theme) => ({
    active: {
        background: "red",
    },
    inactive: {
        background: "yellow",
    },
}));